# PM Application Environment Configuration

# Server Configuration
PORT=3001
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=pm_application

# JWT Configuration
JWT_SECRET=pm-application-secret-key-change-in-production

# CORS Configuration
ALLOWED_ORIGINS=http://localhost:5000,http://localhost:3000

# Logging
LOG_LEVEL=info
